= image_tag(@graph)

%h1 Courses

%div#settings{:align => "right"}
  =link_to 'Total Cache Statistics', total_stats_path, :style => "text-decoration: none"
  /- if @current_user
  =button_to 'Cache Settings', auth_path
  
  

%table#courses.table.table-striped.table-bordered
  %thead
    %tr
      %th Subject
      %th Course Title
  %tbody
    - @courses.each do |course|
      %tr
        %td= course.subject
        %td= link_to "#{course.title}", course_videos_path(course.id) #this is a placeholder path for now
