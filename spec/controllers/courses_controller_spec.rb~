require 'spec_helper'

describe CoursesController do

  describe 'getting to the FreEdu homepage' do
    before :each do
      get :index
    end
    it 'should select the Courses Index template for rendering' do
      response.should render_template('courses/index')
    end
    it 'should make the courses in the database available to the template'
    it 'should direct to a RESTful route'
  end
end
