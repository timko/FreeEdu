= form_tag courses_path, :method => :get, :id => 'stats_form' do
  Include: 
  - @all_stats.each do |stat|
    = @real_stat_names[stat]
    = check_box_tag "stats_#{stat}", 1, (@selected_stats.include?(stat) or @selected_stats == {})
  = submit_tag 'Update', :id => 'stats_submit'

= image_tag(@graph)

%h1 Courses

%div#settings{:align => "right"}
  =link_to 'Total Cache Statistics', total_stats_path
  =button_to 'Cache Settings', settings_path(@user.id), :method => :put #this is a placeholder path for now

%table#courses.table.table-striped.table-bordered
  %thead
    %tr
      %th Subject
      %th Course Title
  %tbody
    - @courses.each do |course|
      %tr
        %td= course.subject
        %td= link_to "#{course.title}", course_videos_path(course.id) #this is a placeholder path for now
